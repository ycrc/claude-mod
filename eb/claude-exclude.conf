# Directories that must not be bound into the Claude container and must not be
# used as Claude's working directory.
#
# Entries must be absolute directories. Shell variables such as $HOME are
# expanded when this administrator-controlled file is loaded. Missing paths are
# skipped, and symlinks are resolved before the working directory is checked.

excluded_workdirs=(
    "$HOME/ondemand"
)
